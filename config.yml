database:
  sqlserver:
    driver: "ODBC Driver 17 for SQL Server"
    server: "1.1.1"
    database: "AYTYRECORDMANAGERDB"
    uid: "user"
    pwd: "password"
  mysql:
    host: "2.2.2.2"
    user: "user"
    password: "password"
    db: "aytyipbxdb"

paths:
  source_rep: "/usr/local/aytytech/var/log/asterisk/aytycrm/recording/copy_rec_success/"
  log_dir: "/usr/local/aytytech/var/log/recording_status_sync/"
  lock_file: "/usr/local/aytytech/var/log/recording_status_sync/rec_status_sync.lock"

retry:
  max_retries: 5
  backoff_seconds: [300, 900, 3600, 21600, 86400]

processing:
  min_file_age_seconds: 300

logging:
  level: "DEBUG"
